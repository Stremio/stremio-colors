name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Install NPM dependencies
      run: npm install
    - name: Build
      run: npm run build
